
name: Jekyll site CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: change directory
      run: cd site
    - name: Jekyll Action
      uses: helaili/jekyll-action@1.0
