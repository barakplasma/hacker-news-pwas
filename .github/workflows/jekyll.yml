
name: Jekyll site CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: change directory
      run: cd site
    - name: build
      run: docker run --rm --volume="$PWD:/srv/jekyll" -d jekyll/builder:latest jekyll build
